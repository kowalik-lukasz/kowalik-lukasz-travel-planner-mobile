#:import MapView kivy.garden.mapview.MapView

<TravelPlannerMapView>:
    lat: 52.13
    lon: 21.00
    zoom: 5
    on_zoom:
        self.zoom = 3 if self.zoom <= 3 else self.zoom
    on_lat:
        if self.zoom >= 7: self.start_getting_markers_in_fov()
    on_lon:
        if self.zoom >= 7: self.start_getting_markers_in_fov()